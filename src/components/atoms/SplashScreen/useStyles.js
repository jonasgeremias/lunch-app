import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
    background: {
        backgroundColor: '#fff',
        display: 'flex',
        height: '100vh',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center'
    }
}))